// Task-1
// Take four parameters. Multiply the four numbers and then return the result

const multiply = (num1, num2, num3, num4) => {
    const sum = num1 * num2 * num3 * num4;
    return sum;
}

const result = multiply(4, 2, 3, 5);
console.log(result);